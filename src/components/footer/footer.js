import React from 'react';
import './footer.css'

const Footer = (props) => {
    return(
        <footer className="footer">
            <div class="container">
                <p>Feito por Estela ♡</p>
            </div>
        </footer>
    );
}

export default Footer;